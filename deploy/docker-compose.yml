services:
  classguard:
    build: .
    image: cognis/classguard:local
    restart: unless-stopped
    ports: ["8000:8000"]
    environment:
      - COGNIS_LOG_LEVEL=info
    volumes: ["./data:/data"]
